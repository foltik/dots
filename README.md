# Dotfiles

All the packages and config to make one of my machines cozy.

* Add/remove packages you want in `setup.py`.
* Run `setup.py`, creating `prefix/`
* Run `rsync.sh $HOST` to sync this prefix to a machine
* Put the line from `ssh_config` in your own local `~/.ssh/config`
* ???
* Profit
